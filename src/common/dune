(library
 (name common)
 (libraries core core_kernel.total_map raylib)
 (preprocess
  (pps ppx_jane)))

(rule
 (target resources.ml)
 (deps
  (source_tree resources))
 (action
  (with-stdout-to
   %{target}
   (run ocaml-crunch -m plain ../../resources))))
